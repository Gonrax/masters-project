<!DOCTYPE html>
<html lang="en">

<head>
    <title>Home</title>
    <link rel="icon" href="/images/logo.png">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <div class="main-content">
        {{>header}}

        <h1>Profile</h1>
        <div class="body">
            <div class="details">

                <p>First Name: {{user.first_name}}</p>
                <p>Last Name: {{user.last_name}}</p>
                <p>Email Address: {{user.email}}</p>

            </div>
            {{#if user.image}}
                <img src="{{user.image}}" height="200px" style="float: right" alt="Profile Picture">
            {{/if}}

        </div>

    </div>
    {{>footer}}
    <script src="/js/app.js"></script>
</body>

</html>