<!DOCTYPE html>
<html lang="en">

<head>
    <title>Job Listings</title>
    <link rel="icon" href="/images/logo.png">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <div class="main-content">
        {{>header}}

        <div class="body">
            <h1>Job Postings</h1>
            <div class="grid-container">
                {{#if jobs.length}}
                    {{#each jobs}}
                        {{>jobsPartial view="bidder" user=../user}}
                    {{/each}}
                {{else}}
                    <h4>There are no auctions created</h4>
                {{/if}}
            </div>
        </div>

    </div>

    {{>footer}}
</body>

</html>